import React from 'react'

type Props = {
    url?: string;
    limit?: number;
}

const ImageSlider = ({url, limit}: Props) => {
  return (
    <div>ImageSlider</div>
  )
}

export default ImageSlider